<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sakaaz.sakaaz">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="edmt.dev.androidsakaaz.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="edmt.dev.androidsakaaz.SignIn" />
        <activity android:name="edmt.dev.androidsakaaz.SignUp" />
        <activity
            android:name="edmt.dev.androidsakaaz.Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:parentActivityName="edmt.dev.androidsakaaz.Home"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edmt.dev.androidsakaaz.Home" />
        </activity>

        <activity android:name="edmt.dev.androidsakaaz.MakeupList" />
        <activity android:name="edmt.dev.androidsakaaz.MakeupDetail" />
        <activity android:name="edmt.dev.androidsakaaz.Cart"/>


    </application>
</manifest>